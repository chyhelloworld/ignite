# 想法引擎(Spark)第一期 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 搭出"想法引擎"第一期核心飞轮——把破碎想法捕捉进本地想法池,经背景调查、完善、联网调研、可行性评估,死掉的想法靠找火花重组。

**Architecture:** 本地文件知识工程 + 7 个 Claude Code 项目级 skill(`.claude/skills/<name>/SKILL.md`)。数据为根目录下的 markdown 文件(一想法一文件);skill 是结构化系统提示词,不是传统代码。唯一可执行件是一个 Node 结构校验器,作为自动化测试门;skill 行为靠"黄金样例 dry-run"人工回归(对应 spec §8)。

**Tech Stack:** Markdown(YAML frontmatter)· Node 22(校验器,纯标准库)· Claude Code 项目级 skill · 联网 MCP(web_search / web_reader,运行时绑定)。

**Spec:** `docs/superpowers/specs/2026-06-04-想法引擎-design.md`(本计划的权威细节来源;各 skill 正文按其 §6/§7 撰写)。

---

## 命名约定(锁定)

skill 用 ascii 名 + `spark-` 前缀,中文触发词写进 description(沿用 dbskill 双语 description 模式,中文路由由主入口 skill 承担)。

| 中文角色 | skill name | 主触发 |
|---|---|---|
| 主入口 | `spark` | `/想法` `/idea` |
| 捕捉 | `spark-capture` | `/捕捉` |
| 完善 | `spark-refine` | `/完善` |
| 背景调查 | `spark-background` | `/背景` |
| 调研 | `spark-research` | `/调研` |
| 评估 | `spark-evaluate` | `/评估` |
| 找火花 | `spark-combine` | `/找火花` |

候选 = `status: candidate` 的 idea 原子,不另起命名空间(spec §4.1 / §5.2)。

---

## File Structure(本期创建/修改的全部文件)

```text
{项目根}/
├── tools/
│   └── validate.mjs                 # [新] Node 结构+frontmatter 校验器(自动化测试门)
├── 想法池/
│   ├── _状态总览.md                  # [新] 第一入口,空表模板
│   └── .gitkeep
├── 调研/.gitkeep                     # [新]
├── 决策档案/.gitkeep                 # [新]
├── 产出/.gitkeep                     # [新]
├── 背景库/
│   ├── 概念词典.md                   # [新] 空表模板
│   ├── _背景索引.md                  # [新] 空表模板
│   └── 项目档案/.gitkeep             # [新]
├── 规则/
│   ├── schema与阶段定义.md           # [新] 字段/状态/来源标签
│   ├── 品类与平台映射.md             # [新] 品类→首发平台默认表
│   ├── 死因词表.md                   # [新] 受控词表
│   └── eval样例.md                   # [新] 黄金样例回归
└── .claude/skills/
    ├── spark/SKILL.md                # [新] 主入口/路由
    ├── spark-capture/SKILL.md        # [新]
    ├── spark-refine/SKILL.md         # [新]
    ├── spark-background/SKILL.md     # [新]
    ├── spark-research/SKILL.md       # [新]
    ├── spark-evaluate/SKILL.md       # [新]
    └── spark-combine/SKILL.md        # [新]
```

**职责边界:** `tools/validate.mjs` 只做契约校验(目录/文件存在 + 每个 SKILL.md 有 name+description);`规则/` 是可配置数据(改它不动 skill);`背景库/` 与 `想法池/` 等是运行期数据。每个 skill 文件单一职责,200–300 行内。

---

## 测试策略(本期如何"测")

1. **自动化门:** `node tools/validate.mjs` —— 退出码 0 = 结构契约 + 所有 skill frontmatter 合法。每个 commit 前必跑。
2. **行为回归(人工):** `规则/eval样例.md` 里的黄金样例。每写完一个 skill,按其样例做一次 dry-run,确认行为符合期望(spec §8)。dry-run 是真实在 Claude Code 里调用该 skill,观察它是否按 phase/闸门动作。
3. skill 正文是提示词,无法 pytest;不为其上重型框架(spec §1.1)。

---

# Phase A:数据契约与脚手架(先把结构钉死,spec D8)

## Task 1: 结构校验器 + 目录骨架

**Files:**
- Create: `tools/validate.mjs`
- Create: `想法池/.gitkeep`, `调研/.gitkeep`, `决策档案/.gitkeep`, `产出/.gitkeep`, `背景库/项目档案/.gitkeep`

- [ ] **Step 1: 写校验器(先让它失败)**

Create `tools/validate.mjs`:

```js
#!/usr/bin/env node
import { existsSync, readdirSync, readFileSync, statSync } from "node:fs";
import { join } from "node:path";

const root = process.cwd();
const errors = [];

// 1) 必需目录
const requiredDirs = [
  "想法池", "调研", "决策档案", "产出",
  "背景库", "背景库/项目档案", "规则", ".claude/skills",
];
for (const d of requiredDirs) {
  if (!existsSync(join(root, d)) || !statSync(join(root, d)).isDirectory()) {
    errors.push(`缺少目录: ${d}`);
  }
}

// 2) 必需文件(Phase A 末尾应全部存在)
const requiredFiles = [
  "想法池/_状态总览.md",
  "背景库/概念词典.md",
  "背景库/_背景索引.md",
  "规则/schema与阶段定义.md",
  "规则/品类与平台映射.md",
  "规则/死因词表.md",
  "规则/eval样例.md",
];
for (const f of requiredFiles) {
  if (!existsSync(join(root, f))) errors.push(`缺少文件: ${f}`);
}

// 3) 每个 skill 目录必须有带 name + description 的 SKILL.md
const skillsDir = join(root, ".claude/skills");
if (existsSync(skillsDir)) {
  for (const name of readdirSync(skillsDir)) {
    const p = join(skillsDir, name, "SKILL.md");
    if (!existsSync(p)) { errors.push(`skill 缺 SKILL.md: ${name}`); continue; }
    const text = readFileSync(p, "utf8");
    const fm = text.match(/^---\n([\s\S]*?)\n---/);
    if (!fm) { errors.push(`${name}: 缺 YAML frontmatter`); continue; }
    if (!/\bname:\s*\S/.test(fm[1])) errors.push(`${name}: frontmatter 缺 name`);
    if (!/\bdescription:\s*[\S|]/.test(fm[1])) errors.push(`${name}: frontmatter 缺 description`);
  }
}

if (errors.length) {
  console.error("FAIL\n" + errors.map((e) => "  - " + e).join("\n"));
  process.exit(1);
}
console.log("OK: 结构契约与 skill frontmatter 全部通过");
```

- [ ] **Step 2: 跑校验器,确认失败**

Run: `node tools/validate.mjs`
Expected: FAIL,列出缺少的目录/文件(此时一个都没建)。

- [ ] **Step 3: 建目录骨架**

Run:
```bash
mkdir -p 想法池 调研 决策档案 产出 背景库/项目档案 规则 .claude/skills
touch 想法池/.gitkeep 调研/.gitkeep 决策档案/.gitkeep 产出/.gitkeep 背景库/项目档案/.gitkeep
```

- [ ] **Step 4: 再跑,确认目录过、文件仍缺**

Run: `node tools/validate.mjs`
Expected: FAIL,但只剩"缺少文件:…"(7 个规则/背景文件),目录类报错消失。

- [ ] **Step 5: 提交**

```bash
git add tools/validate.mjs 想法池/.gitkeep 调研/.gitkeep 决策档案/.gitkeep 产出/.gitkeep 背景库/项目档案/.gitkeep
git commit -m "feat: 结构校验器 + 想法引擎目录骨架"
```

---

## Task 2: 规则层配置文件(可配置数据)

**Files:**
- Create: `规则/schema与阶段定义.md`, `规则/品类与平台映射.md`, `规则/死因词表.md`, `规则/eval样例.md`

- [ ] **Step 1: 写 `规则/schema与阶段定义.md`**(全文)

````markdown
# Schema 与阶段定义

## 想法原子 frontmatter 字段

| 字段 | 取值 | 说明 |
|---|---|---|
| id | `idea-NNNN` | 四位序号,递增,不复用 |
| title | string | 一句话标题 |
| status | raw/refining/refined/researching/researched/evaluated/candidate/documented | 当前阶段 |
| kind | single/combo | combo 由找火花产生 |
| parents | `[idea-..]` | combo 的来源原子;single 为 `[]` |
| tags | `[..]` | 主题标签,找火花聚类用 |
| category | 游戏/App/SaaS/内容/硬件/工具/… | 品类 |
| platform | Steam/iOS/Android/Web/小程序/抖音/公众号/… | 首发平台 |
| effort | S/M/L/XL | 工作量预估 |
| excitement | 1-5 | 用户主观兴奋度 |
| verdict | null/go/no-go/maybe | 评估门结论 |
| death_reason | null 或死因词 | no-go 时必填,取自死因词表 |
| created / updated | YYYY-MM-DD | 日期 |
| research_ref / eval_ref | 路径或 null | 关联快照 |
| artifacts | `[..]` | 产出文件 |
| relationships | `[{type,target}]` | 互补/撞车/同源/反例 |

## 状态流转

raw →(完善)→ refined →(调研)→ researched →(评估)→ evaluated
  ├ verdict=go → candidate →(商业/PRD,二期)→ documented
  ├ verdict=maybe → 回调研/回完善
  └ verdict=no-go → 留池,death_reason 必填,成为找火花燃料

## 来源标签(写进正文)

`[本人]` 用户原话 · `[AI推测]` 未证实推断 · `[AI结论]` 推理结论 · `[修正]` 后续更正 · `[已核实]` 带来源链接的事实 · `[未核实]` 无来源 · `[存疑]` 矛盾未决

## 想法原子 body 固定结构

```
## 原文 [本人]
## 完善后描述
## 核心假设
## 待调研
## 完善记录 [AI 元记录]
```
````

- [ ] **Step 2: 写 `规则/品类与平台映射.md`**(全文)

```markdown
# 品类 → 首发平台 默认映射

> 完善阶段按品类自动填 platform,用户可逐条覆盖。可自由增删。

| 品类 | 默认首发平台 | 调研主源 |
|---|---|---|
| 游戏 | Steam | Steam 同类标签榜、SteamDB |
| App | iOS | App Store 榜单、类似 app |
| SaaS | Web | 通搜 + Product Hunt |
| 内容 | 抖音 | 平台搜索 + 对标账号 |
| 工具 | Web | 通搜 + Product Hunt + GitHub |
| 硬件 | 众筹 | 京东众筹/嘉立创 + 电商榜 |

可选平台清单:Steam / iOS / Android / Web / 小程序 / 抖音 / 公众号 / 众筹 / 微信 / 桌面端。
```

- [ ] **Step 3: 写 `规则/死因词表.md`**(全文)

```markdown
# 死因词表(受控)

> 评估门给 no-go 时,death_reason 必须取自此表。找火花按死因互补匹配。可扩展。

| 死因 | 含义 | 可被什么互补 |
|---|---|---|
| 缺流量入口 | 做得出但没人知道 | 自带流量/渠道的想法 |
| 缺变现路径 | 有人用但收不到钱 | 有付费场景的想法 |
| 缺技术能力 | 实现门槛超出当前能力 | 已有技术资产的想法 |
| 撞车无差异 | 竞品已做得很好且无差异化 | 提供差异化角度的想法 |
| 需求太弱 | 痛点不够痛,没人真买 | 强需求场景的想法 |
| effort过大 | 投入远超可承受 | 能复用/降本的想法 |
| 平台限制 | 平台政策/抽成/审核卡死 | 换平台或绕开的想法 |
```

- [ ] **Step 4: 写 `规则/eval样例.md`**(全文,行为回归基准)

```markdown
# 黄金样例回归

> 每改一个 skill,跑相关样例做 dry-run,确认行为。这是本系统唯一的"测试"。

## S1 伪概念应被降级
输入:`/捕捉 "我想做个 AI 改变世界"` 然后 `/完善`
期望:完善在语言陷阱检测判定核心词无法定义 → 标 `#太空泛`,status 不进 researching,不触发调研。

## S2 清晰想法走通主线
输入:`/捕捉 "给 Steam 独立游戏做差评聚合工具"` → `/完善` → `/调研` → `/评估`
期望:完善出闸(category=游戏,platform=Steam 自动填,≥1 假设,≥2 关键词);调研扒到竞品且每条带来源;评估给出 verdict。

## S3 撞车应 no-go 且写死因
输入:一个与现有成熟竞品高度重合、无差异化的想法,走到 `/评估`
期望:评估识别撞车,verdict=no-go,death_reason=`撞车无差异`,原子留池。

## S4 互补死原子应被配对
前置:池中有两个 no-go 原子,death_reason 分别为 `缺流量入口`、`缺变现路径`
输入:`/找火花`
期望:把这两个配成 combo 候选(kind=combo,parents 含两者),建新 idea 原子。

## S5 未知术语应被背景调查
输入:`/捕捉 "做个 roguelike 的 SteamDB"` → `/完善`
期望:背景调查标记 `roguelike`、`SteamDB` 为不确定,征求同意 → 同意后联网查 → 写进 `背景库/概念词典.md`(带来源+日期)。
```

- [ ] **Step 5: 跑校验器**

Run: `node tools/validate.mjs`
Expected: 仍 FAIL,但只剩"缺少文件:想法池/_状态总览.md、背景库/概念词典.md、背景库/_背景索引.md"(规则 4 文件已过)。

- [ ] **Step 6: 提交**

```bash
git add 规则/
git commit -m "feat: 规则层配置(schema/平台映射/死因词表/eval样例)"
```

---

## Task 3: 背景库与状态总览模板

**Files:**
- Create: `背景库/概念词典.md`, `背景库/_背景索引.md`, `想法池/_状态总览.md`

- [ ] **Step 1: 写 `背景库/概念词典.md`**(空表模板)

```markdown
# 概念词典

> 公开术语,对话中实时生长。每条由背景调查写入,必须带来源+日期。

| 术语 | 一句话定义 | 关键事实 | 来源 | 日期 | 标签 |
|---|---|---|---|---|---|
```

- [ ] **Step 2: 写 `背景库/_背景索引.md`**(空表模板)

```markdown
# 背景索引

> 所有 skill 开头读此文件,快速抓核心。术语指向概念词典行,项目指向项目档案文件。

## 术语
| 术语 | 一句话 |
|---|---|

## 项目
| 项目 | 一句话 | 档案 |
|---|---|---|
```

- [ ] **Step 3: 写 `想法池/_状态总览.md`**(空表模板)

```markdown
# 状态总览

> 第一入口。每个 skill 收尾更新自己那一行。可按 excitement 降序 / effort 升序看该先动哪个。

| id | 标题 | status | verdict | category | platform | excitement | effort | updated |
|---|---|---|---|---|---|---|---|---|
```

- [ ] **Step 4: 跑校验器,确认全过**

Run: `node tools/validate.mjs`
Expected: `OK: 结构契约与 skill frontmatter 全部通过`(此时 skills 目录为空,skill 校验段不报错)。退出码 0。

- [ ] **Step 5: 提交**

```bash
git add 背景库/概念词典.md 背景库/_背景索引.md 想法池/_状态总览.md
git commit -m "feat: 背景库与状态总览模板"
```

---

# Phase B:核心 skill(每个独立可用增量)

> 通用步骤:每个 skill 任务都 ①写 SKILL.md ②跑 `node tools/validate.mjs`(校验 frontmatter)③按其黄金样例 dry-run ④提交。
> 各 skill 正文以列出的"必含章节 + 不可协商规则"为准,细节展开依据 spec 对应小节。description 必须中英双语 + 列中文触发词(dbskill 模式)。

## Task 4: 主入口 `spark`(路由)

**Files:**
- Create: `.claude/skills/spark/SKILL.md`

- [ ] **Step 1: 写 SKILL.md**

Frontmatter(完整):
```yaml
---
name: spark
description: |
  想法引擎主入口。把你的请求路由到对的 skill,只路由不干活。
  触发:/想法、/idea、「帮我看看这个想法」
  Spark idea-engine router. Routes to the right skill; does not do the work itself.
---
```

正文必含章节(依据 spec §3、§5.1、§5.3):
1. `# spark:想法引擎入口` + 一句话:**只做路由,不捕捉/不调研/不评估**。
2. `## 开头必读`:先读 `背景库/_背景索引.md` 与 `想法池/_状态总览.md`,据此理解上下文。
3. `## 路由表`:意图信号 → skill 的表,覆盖 7 个第一期 skill(捕捉/完善/背景/调研/评估/找火花)+ 二期占位(商业/PRD 暂答"二期再做")。
4. `## 工作流程`:明确需求直接路由;模糊则按状态总览给候选清单让用户选;确认后说一句"交给 {skill}"再执行。
5. `## 边界`:多需求→一个一个来;闲聊不接。

不可协商规则:**主入口绝不自己产出想法/调研/评估内容**;路由前必读索引与状态总览。

- [ ] **Step 2: 校验**

Run: `node tools/validate.mjs`
Expected: OK(spark frontmatter 合法)。

- [ ] **Step 3: dry-run**

在 Claude Code 输入 `/想法`,给一句模糊请求。期望:它读状态总览、给出可选去向、不自己动手分析。

- [ ] **Step 4: 提交**

```bash
git add .claude/skills/spark/SKILL.md
git commit -m "feat: spark 主入口路由 skill"
```

---

## Task 5: 捕捉 `spark-capture`

**Files:**
- Create: `.claude/skills/spark-capture/SKILL.md`

- [ ] **Step 1: 写 SKILL.md**

Frontmatter:
```yaml
---
name: spark-capture
description: |
  想法引擎·捕捉。零摩擦把一句话想法存进想法池,原文逐字保留。
  触发:/捕捉、/capture、「记一下」「先存着这个想法」
  Spark capture: append a raw idea atom to the pool with zero friction.
---
```

正文必含章节(依据 spec §4.2/§4.3、§5.1、§6.3 捕捉零摩擦原则):
1. 职责:**只建 raw 原子,不追问、不养肥、不调研**。
2. `## 建原子流程`:取下一个 `idea-NNNN`(扫 `想法池/` 现有最大序号 +1);文件名 `idea-NNNN_标题.md`;写入完整 frontmatter(status=raw,未知字段填 null/空,created=updated=今天);body 至少写 `## 原文 [本人]` 逐字保留用户那句话。
3. `## 零摩擦原则`:若句中有疑似未知术语,**不打断**,只在 body 记一行 `待补背景:[词]`,留给完善或 `/背景`。
4. `## 收尾`:更新 `想法池/_状态总览.md` 加一行;回执一句(id + 路径 + "下一步 /完善")。
5. `## 边界`:空输入不建;一次多个想法→拆成多条分别建。

不可协商规则:原文逐字保留不改写;不在捕捉阶段联网或追问。

- [ ] **Step 2: 校验** — Run: `node tools/validate.mjs` → OK
- [ ] **Step 3: dry-run** — `/捕捉 "给 Steam 独立游戏做差评聚合工具"`;确认生成 `想法池/idea-0001_*.md`,frontmatter 完整、status=raw、状态总览新增一行。
- [ ] **Step 4: 提交**

```bash
git add .claude/skills/spark-capture/SKILL.md
git commit -m "feat: spark-capture 捕捉 skill"
```

---

## Task 6: 完善 `spark-refine`

**Files:**
- Create: `.claude/skills/spark-refine/SKILL.md`

- [ ] **Step 1: 写 SKILL.md**

Frontmatter:
```yaml
---
name: spark-refine
description: |
  想法引擎·完善。交互式对话把一句话养肥到可调研,太空泛/伪概念当场降级,收尾找火花。
  触发:/完善、/refine、「帮我把这个想法想清楚」
  Spark refine: interactively flesh out a raw idea into a researchable one.
---
```

正文必含章节(依据 spec §6.1,**逐条照搬其 7 个 Phase 与闸门**):
1. 职责 + 开头读 `背景库/_背景索引`、目标 idea 文件、`规则/品类与平台映射.md`。
2. `## Phase 0–7`:复述→问"谁的什么痛"→语言陷阱检测(借 diagnosis 逼定义)→产品形态+按映射表填 category/platform→抽 1-3 条核心假设→问 excitement+估 effort→列待调研(竞品关键词+待核实事实)→收尾调用找火花扫池。
3. `## 出闸条件`(四条全列):一句话说清为谁解决什么 / category+platform 已定 / ≥1 核心假设 / ≥2 竞品关键词。满足才 status=refined。
4. `## 降级判据`:核心词无法定义 `#太空泛`/`#伪概念`、说不出痛点、本质是情绪愿望非产品 → 留池不进调研。
5. `## 背景钩子`:遇 body 里的 `待补背景:` 或对话中不确定术语 → 提示走 `/背景`(标记+确认)。
6. `## 对话纪律`:一次问一个,停下等回应(照搬 dbskill);收尾更新原子 + 状态总览。

不可协商规则:**一次只问一个问题**;不满足出闸四条不得标 refined;降级不删除原子。

- [ ] **Step 2: 校验** — `node tools/validate.mjs` → OK
- [ ] **Step 3: dry-run** — 跑样例 S1(伪概念应降级)+ S2 的完善段(清晰想法应出闸、自动填 Steam)。确认两种分支行为正确。
- [ ] **Step 4: 提交**

```bash
git add .claude/skills/spark-refine/SKILL.md
git commit -m "feat: spark-refine 完善 skill"
```

---

## Task 7: 背景调查 `spark-background`

**Files:**
- Create: `.claude/skills/spark-background/SKILL.md`

- [ ] **Step 1: 写 SKILL.md**

Frontmatter:
```yaml
---
name: spark-background
description: |
  想法引擎·背景调查。标记不懂的术语/项目背景,征得同意后:公开术语联网查、私有项目问你,沉淀进背景库。
  触发:/背景、/background、「这几个词你懂吗」
  Spark background: flag unknown terms/projects, then research or ask, and persist to the background library.
---
```

正文必含章节(依据 spec §6.3,**逐条照搬 Phase 0–4**):
1. 职责 + 开头读 `背景库/_背景索引`(已沉淀不重复查)。
2. `## Phase 0–4`:识别不确定术语/项目→分类(公开/私有)→**标记并征求同意**("这几个我不确定:A 联网查?B 你讲两句?")→同意后:公开术语 web_search 定义+关键事实带来源写 `概念词典.md`([已核实]+日期);私有背景问 3 点(是什么/现状/为何相关)写 `项目档案/proj-xxx.md`([本人])→更新 `_背景索引` 回原对话。
3. `## 触发约定`:可手动 `/背景`,也被完善/捕捉调用;**未经同意不联网、不长篇追问**。
4. `## 写入规范`:概念词典一行一术语;项目档案含"是什么/现状/约束/相关人(代号)"。

不可协商规则:**标记+确认后才行动**(spec D10);公开事实无来源标 `[未核实]`,不脑补。

- [ ] **Step 2: 校验** — `node tools/validate.mjs` → OK
- [ ] **Step 3: dry-run** — 跑样例 S5(`roguelike`/`SteamDB`)。确认:先列出待查项征求同意 → 同意后联网 → 概念词典新增带来源行 → 背景索引更新。
- [ ] **Step 4: 提交**

```bash
git add .claude/skills/spark-background/SKILL.md
git commit -m "feat: spark-background 背景调查 skill"
```

---

## Task 8: 调研 `spark-research`

**Files:**
- Create: `.claude/skills/spark-research/SKILL.md`

- [ ] **Step 1: 写 SKILL.md**

Frontmatter:
```yaml
---
name: spark-research
description: |
  想法引擎·调研。按品类/平台联网扒真实竞品(产品/定价/体量/差评痛点),每条带来源,绝不脑补。
  触发:/调研、/research、「查查竞品」
  Spark research: live competitor research by category/platform with sourced facts only.
---
```

正文必含章节(依据 spec §7,**照搬 Phase 0–5 + 防幻觉 + 错误处理**):
1. 职责 + 前置:目标 idea 须 status≥refined;读 `规则/品类与平台映射.md` 决定调研主源。
2. `## Phase 0–5`:定市场→web_search 找竞品名单→web_reader 逐个深挖(做什么/定价/体量/融资/差评痛点)→信息分级 `[已核实+链接]/[AI推测]/[未核实]`→写 `调研/idea-NNNN_调研.md` 回填 research_ref、status=researched→留钩子(价格带/最痛差评/市场空白)。
3. `## 报告结构`:竞品清单表(竞品/做什么/定价/体量/**来源**)→价格带→用户痛点→空白与差异化→信息可信度说明。
4. `## 防幻觉硬约束`:无来源链接的竞品事实一律 `[未核实]`,数字带来源+日期。
5. `## 错误处理`(三类):搜不到直接竞品=强信号(写明,交评估判,不默认蓝海);web 工具失败=标"调研未完成"、status 不推进、提示重试、**绝不脑补冒充**;信息矛盾=两条都列各带来源标 `[存疑]`。

不可协商规则:只写带来源的事实当 `[已核实]`;工具失败不得用模型记忆冒充联网结果;收尾更新状态总览。

- [ ] **Step 2: 校验** — `node tools/validate.mjs` → OK
- [ ] **Step 3: dry-run** — 对 S2 的 idea 跑 `/调研`。确认生成调研报告、竞品每条带来源链接、status→researched、research_ref 回填。
- [ ] **Step 4: 提交**

```bash
git add .claude/skills/spark-research/SKILL.md
git commit -m "feat: spark-research 调研 skill"
```

---

## Task 9: 评估 `spark-evaluate`

**Files:**
- Create: `.claude/skills/spark-evaluate/SKILL.md`

- [ ] **Step 1: 写 SKILL.md**

Frontmatter:
```yaml
---
name: spark-evaluate
description: |
  想法引擎·评估门。逐条挑战核心假设出 verdict(go/no-go/maybe);no-go 降级回池并写死因。
  触发:/评估、/evaluate、「这个能不能做」
  Spark evaluate: feasibility gate; no-go demotes the idea to the pool with a death reason.
---
```

正文必含章节(依据 spec §6.2,**照搬 Phase 0–5 + 死因机制**):
1. 职责 + 前置:无 research_ref 直接拦"先 /调研"。
2. `## Phase 0–5`:读 idea+调研报告→逐条挑战核心假设(标 成立/存疑/证伪,**必须引 [已核实] 证据**)→竞品现实核对(撞车检测)→商业快筛(借 diagnosis 公理:真实付费意愿/effort vs 资源/平台现实)→出 verdict→写评估快照(写完不改+时间戳)回填 verdict/eval_ref。
3. `## verdict 判据`:go=假设多数成立+有差异化+effort 可承受→status=candidate;maybe=关键假设缺数据→回调研/回完善;no-go=假设被证伪/撞车无差异/effort 过大→留池。
4. `## 死因机制`:no-go **必填 death_reason**,取自 `规则/死因词表.md`;说明死因即找火花燃料。
5. `## 收尾`:更新状态总览;go 则提示二期 `/商业`。

不可协商规则:无调研不评估;no-go 必写死因且**不删除原子**;评估快照写完不改。

- [ ] **Step 2: 校验** — `node tools/validate.mjs` → OK
- [ ] **Step 3: dry-run** — 跑样例 S3(撞车→no-go→death_reason=撞车无差异)。确认快照落 `决策档案/`、verdict/eval_ref/death_reason 回填、原子仍在池中。
- [ ] **Step 4: 提交**

```bash
git add .claude/skills/spark-evaluate/SKILL.md
git commit -m "feat: spark-evaluate 评估门 skill"
```

---

## Task 10: 找火花 `spark-combine`

**Files:**
- Create: `.claude/skills/spark-combine/SKILL.md`

- [ ] **Step 1: 写 SKILL.md**

Frontmatter:
```yaml
---
name: spark-combine
description: |
  想法引擎·找火花。扫全池(含死掉的想法)按主题/死因互补提组合,建 combo 候选。
  触发:/找火花、/combine、「拼拼看有没有火花」
  Spark combine: scan the whole pool (incl. dead atoms) and propose combinations as new combo candidates.
---
```

正文必含章节(依据 spec §3、§5.1、§6.2 死因机制):
1. 职责 + 两种触发:`/找火花`=全池扫描;被完善调用=轻量只扫与当前想法相关。
2. `## 扫描流程`:读全 `想法池/`(**含 no-go 死原子**)→按 tags 主题邻近 + death_reason 互补(查 `规则/死因词表.md` 的"可被什么互补")配对→给出"A+C 可能擦出 X"候选及理由。
3. `## 建 combo`:用户选中后,新建 `idea-NNNN`,kind=combo,parents=来源 id,status=raw,原文写"由 A+C 组合";**原 A/C 原封不动**;在各原子 relationships 互记关系。
4. `## 收尾`:提示新 combo 走 `/完善` 重新养;更新状态总览。

不可协商规则:死原子是燃料不可忽略;组合产生新原子而非改写来源;新 combo 必回完善。

- [ ] **Step 2: 校验** — `node tools/validate.mjs` → OK
- [ ] **Step 3: dry-run** — 跑样例 S4(两个互补死原子)。确认配对建出 combo 原子、parents 正确、原子互记关系、提示去完善。
- [ ] **Step 4: 提交**

```bash
git add .claude/skills/spark-combine/SKILL.md
git commit -m "feat: spark-combine 找火花 skill"
```

---

## Task 11: 端到端贯通验收

**Files:** 无新建,跑全链路。

- [ ] **Step 1: 全链路 dry-run**

依次:`/捕捉 "给 Steam 独立游戏做差评聚合工具"` → `/完善`(确认自动填 Steam、出闸)→ 若遇术语走 `/背景` → `/调研` → `/评估`。确认每步文件落盘、status 流转、状态总览同步。

- [ ] **Step 2: 死亡重组 dry-run**

构造两个 no-go 互补原子(death_reason=缺流量入口 / 缺变现路径)→ `/找火花` → 生成 combo → `/完善` 重新养。确认闭环成立。

- [ ] **Step 3: 校验 + 提交收尾**

Run: `node tools/validate.mjs` → OK。
```bash
git add -A
git commit -m "chore: 第一期端到端贯通验收"
```

---

## Self-Review(against spec)

- **Spec coverage:** §3 流水线→Tasks 4-10 全覆盖;§4 数据模型→Tasks 1-3;§5.1 七 skill→Tasks 4-10;§6.1 完善→Task 6;§6.2 评估+死因→Task 9;§6.3 背景调查→Task 7;§7 调研+防幻觉+错误处理→Task 8;§8 测试→`规则/eval样例.md`(Task 2)+各 dry-run;§9 第一期范围→本计划全部。二期(商业/PRD)按 spec §9 不在本计划,留待下一计划。
- **Placeholder scan:** 无 TODO/TBD;校验器为完整可运行代码;各规则文件全文给出;skill 正文以"必含章节+不可协商规则+引用 spec 小节"约束(prose 制品的等价完整度),非占位。
- **Type/契约一致性:** frontmatter 字段在 `规则/schema与阶段定义.md`(Task 2)定义,后续 skill 任务一致引用;skill name 与命名约定表一致;`tools/validate.mjs` 的必需文件清单与 Task 2/3 产出一致;status/verdict/death_reason 取值跨任务一致。
```